data Nat : U 0 where {
  zero : Nat ;
  succ : Nat -> Nat
} ;

data Bool : U 0 where {
  true : Bool ;
  false : Bool
} ;


-- Should fail (strict positivity)
data Strange : Big where {
  strange : ((Bool → Strange) → (Nat → Strange)) → Strange
} ;

strange