data Nat : U 0 where {
  zero : Nat ;
  succ : Nat -> Nat
} ;

data BadList : Big where {
  nil : BadList ;
  cons : Big -> BadList -> BadList
} ;

-- should fail
let illegalType : U 0 = BadList;

illegalType