data Nat : U 0 where {
  zero : Nat ;
  succ : Nat -> Nat
} ;

data List (A : Big) : Big where {
  nil : List A ;
  cons : A -> List A -> List A
} ;

-- should fail (nested inductive)
data RoseTree (A : Big) : Big where {
  node : A -> List (RoseTree A) -> RoseTree A
} ;

elimRoseTree