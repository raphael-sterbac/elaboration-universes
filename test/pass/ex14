let id : ∀ l. (A : U l) -> A -> A = 
  Λ l. \A x. x ;

let smallId : (A : U 0) -> A -> A = 
  id {0} ;

let largeId : (A : Tp) -> A -> A = 
  id {Tp} ;

let cumul : ∀ l. U 1 -> U l+1 = 
  Λ l. \X. X ;

cumul {Tp} (U 0)