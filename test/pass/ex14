let id : ∀ l. (A : U l) -> A -> A = 
  Λ l. \A x. x ;

let smallId : (A : U 0) -> A -> A = 
  id {0} ;

let largeId : (A : Big) -> A -> A = 
  id {Big} ;

let cumul : ∀ l. U 1 -> U l+1 = 
  Λ l. \X. X ;

cumul {Big} (U 0)