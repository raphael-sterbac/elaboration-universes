data Nat : U 0 where {
  zero : Nat ;
  succ : Nat -> Nat
} ;

-- should fail
let TypeComputer : Nat -> U 1 = \n.
  elimNat (\_. U 1) 
    < \p ih. Nat ,
    < \p ih. (Nat -> Nat) ,
    * > >
    n ;

TypeComputer zero